{
  "name": "@kanbaan/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply kanbaan-catalog --local",
    "db:migrate": "wrangler d1 migrations apply kanbaan-catalog --remote"
  },
  "dependencies": {
    "@kanbaan/contract": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260619.1",
    "wrangler": "^4.103.0"
  }
}
