{
  "name": "kaambaan",
  "version": "0.0.0",
  "private": true,
  "description": "A multi-tenant Kanban board that orchestrates external AI agents through pipeline stages with human approval gates.",
  "packageManager": "pnpm@11.5.2",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
